machine:
  node:
    version: 8.1.4
deployment:
  production:
    branch: master
    commands:
      - git config --global user.email $GH_EMAIL
      - git config --global user.name $GH_NAME
      - npm run deploy
