machine:
  node:
    version: 8.1.4
deployment:
  production:
    branch: master
    commands:
      - cd ~/clean-slate-web
      - git config user.email $GH_EMAIL
      - git config user.name $GH_NAME
      - npm run deploy
